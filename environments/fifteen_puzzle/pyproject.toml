[project]
name = "fifteen-puzzle"
version = "0.1.0"
description = "fifteen-puzzle."
requires-python = ">=3.11"
dependencies = ["datasets", "verifiers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["fifteen_puzzle"]
